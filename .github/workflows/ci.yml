name: ci

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 1.6+ нужен ради terraform test; сам модуль объявляет >= 1.5.
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"
          terraform_wrapper: false

      - name: fmt
        run: terraform fmt -recursive -check -diff

      - name: init
        run: terraform init -backend=false -input=false

      - name: validate
        run: terraform validate

      # Главная проверка. terraform validate не типизирует each.value, поэтому
      # обращение к несуществующему полю объекта он пропускает — ловится только
      # на plan, а тесты как раз планируют. Живой Vault не нужен: все прогоны
      # command = plan, провайдер сконфигурирован фиктивным токеном.
      - name: test
        run: terraform test

  examples:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        example: [minimal, jwt, service]
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"
          terraform_wrapper: false

      - name: validate ${{ matrix.example }}
        working-directory: examples/${{ matrix.example }}
        run: |
          terraform init -backend=false -input=false
          terraform validate
